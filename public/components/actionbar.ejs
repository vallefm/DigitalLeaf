<% if (context == 'project') {%>
  <div class="btn-group mb-4">
    <a class="btn btn-dark <% if (page == 'project') {%> text-leaf-green <%}%>" href="/project/<%-project.id%>">Announcements</a>
    <a class="btn btn-dark <% if (page == 'tasks') {%> text-leaf-green <%}%>" href="/project/<%-project.id %>/tasks">Tasks</a>
    <a class="btn btn-dark <% if (page == 'schedule') {%> text-leaf-green <%}%>" href="/schedule">Schedule</a>
    <a class="btn btn-dark <% if (page == 'team') {%> text-leaf-green <%}%>" href="/project/<%-project.id%>">Team</a>
    <!-- <button type="button" class="btn btn-dark" href="">+</button> -->
  </div>
<% } %>

<% if (context == 'user') {%>
  <div class="btn-group mb-4">
    <a class="btn btn-dark <% if (page == 'home') {%> text-leaf-green <%}%>" href="../<%-firstName %>/home">Home</a>
    <a class="btn btn-dark <% if (page == 'projects') {%> text-leaf-green <%}%>" href="../<%-firstName %>/projects">Projects</a>
    <a class="btn btn-dark <% if (page == 'schedule') {%> text-leaf-green <%}%>" href="../schedule">Schedule</a>
    <!-- <button type="button" class="btn btn-dark" href="">+</button> -->
  </div>
<% } %>